{
  "scripts": {
    "lint": "pnpm oxlint --fix --fix-suggestions -c ./.oxlintrc.json --type-aware",
    "format": "pnpm oxfmt -c ./.oxfmtrc.json",
    "prepare": "husky"
  },
  "dependencies": {
    "date-fns": "catalog:",
    "iso8601-duration": "catalog:",
    "ofetch": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "husky": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:"
  }
}
